<h1> Hello my friend! </h1>

<% @animals.each do |animal| %>
	<p> 
	Name: <%= link_to animal.name, animal_path(animal)%>
	(<%= animal.species %>) 
	<%= link_to 'Edit', edit_animal_path(animal) %></p>

	<%end%>

<%= link_to 'New Animal', new_animal_path(@animal) %>